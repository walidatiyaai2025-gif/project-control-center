# Canonical Project Status

Each managed project gets exactly one authoritative status document in this directory (or a formally declared canonical path mirrored here). Use `templates/PROJECT_STATUS.yml` and `schemas/project-status.schema.json`.

Do not create a status file for an existing project before live discovery establishes its production/development lineage. Unknown values remain `null`; they are never guessed.
